= Known Issues
ifndef::env-site,env-github[]
include::_attributes.adoc[]
endif::[]

The pilot has the following known issue:

 * Images uploaded from the guided setup may not render correctly if they extend the maximum size for height and width. 